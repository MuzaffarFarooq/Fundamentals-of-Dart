void main() {
  var a; // variable declaration
  // a dynamic variable in which everytype of data can be stored.
  a = 5; // int type value is stored in a.
  print(a);
  print(a.runtimeType); // it shows the run time data typeof a

  var b; // variable declaration
  // a dynamic variable in which everytype of data can be stored.
  b = 55.838; // double type value is stored in b.
  print(b);
  print(b.runtimeType); // it shows the run time data typeof b

  var c; // variable declaration
  // a dynamic variable in which everytype of data can be stored.
  c = "MFS"; // String type value is stored in c.
  print(c);
  print(c.runtimeType); // it shows the run time data typeof c

  var d; // variable declaration
  // a dynamic variable in which everytype of data can be stored.
  d = false; // bool type value is stored in d.
  print(d);
  print(d.runtimeType); // it shows the run time data typeof d
}
